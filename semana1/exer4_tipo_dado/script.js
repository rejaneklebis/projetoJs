let dado = prompt("Digite um dado qualquer:");

if (confirm("Deseja verificar o tipo do dado informado?")) {
   
    alert(typeof(dado));
} else {
    
    alert("<h2>Obrigado por visitar esta página</h2>");
}
